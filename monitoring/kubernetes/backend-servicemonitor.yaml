apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: smart-inventory-backend
  labels:
    release: monitoring
spec:
  jobLabel: app
  selector:
    matchLabels:
      app: backend
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
